<?xml version="1.0" encoding="utf-8"?>
<!--
    DEBUG BUILDS ONLY (src/debug overrides src/main/res/xml/network_security_config.xml).
    Same HTTPS-only policy as release, plus cleartext to the local QA backend:
    10.0.2.2 = host machine from the Android emulator, localhost/127.0.0.1 for `adb reverse`.
    Release builds never include this file.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
