<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Shown behind the system bars when the WebView is inset (older WebViews); edge-to-edge on Android 15+. -->
        <item name="android:windowBackground">@color/s2w_page</item>
        <item name="android:navigationBarColor">@color/s2w_page</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">@bool/s2w_light_bars</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Plain page colour + the brand mark; BootSplash takes over pixel-for-pixel. -->
        <item name="windowSplashScreenBackground">@color/s2w_page</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>